@startuml

start

:User opens application;
:Enter username and password;

if (Credentials valid?) then (Yes)
    :Login successful;
    :Show Dashboard;
else (No)
    :Show error message;
    :Ask user to try again;
endif

stop

@enduml
